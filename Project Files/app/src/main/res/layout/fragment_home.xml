<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="16dp"
    android:background="#F5EFFF"
    tools:context=".HomeFragment">

    <!--  Title  -->
    <TextView
        android:id="@+id/titleText"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp"
        android:layout_marginBottom="16dp"
        android:fontFamily="@font/montserrat_bold"
        android:gravity="center"
        android:letterSpacing="0.1"
        android:padding="12dp"
        android:shadowColor="#4A235A"
        android:shadowDx="3"
        android:shadowDy="3"
        android:shadowRadius="6"
        android:text="MALAYSIA CENTER DONATION"
        android:textColor="#C780FA"
        android:textSize="19sp"
        android:textStyle="bold" /> <!-- Custom font -->

    <!-- Search Bar for filtering posts -->
    <androidx.appcompat.widget.SearchView
        android:id="@+id/searchBar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:queryHint="Search for disasters"
        android:iconifiedByDefault="false"
        android:padding="12dp"
        android:layout_marginBottom="16dp"
        android:background="@drawable/searchview_background"
        android:textColorHint="@android:color/darker_gray"
    app:queryBackground="@android:color/transparent"/>



    <!-- Post List -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/postList"
        android:layout_width="match_parent"
        android:layout_height="650dp"
        android:scrollbars="vertical" />

</LinearLayout>
